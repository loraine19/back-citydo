
export enum EventFilter {
    MINE = 'MINE',
    IGO = 'IGO',
    VALIDATED = 'VALIDATE'
}

export enum ServiceUpdate {
    POST_RESP = 'POST_RESP',
    VALID_RESP = 'VALIDATE_RESP',
    CANCEL_RESP = 'CANCEL_RESP',
    FINISH = 'FINISH',
}